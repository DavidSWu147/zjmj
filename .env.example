# Copy to .env (gitignored) and fill in from PlayFab Game Manager.
# Without these the server runs in guest-only fallback mode (no accounts,
# no server-side settings; guest data keyed by device id until PlayFab returns).
PLAYFAB_TITLE_ID=
PLAYFAB_SECRET_KEY=
